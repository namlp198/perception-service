# Integration tests

Hardware integration tests live here and must be explicitly enabled on a Jetson with a D435i attached.
Planned V0.1 coverage includes stream profiles, USB disconnect/reconnect, RTSP client reconnect and
hardware-encoder verification. These tests must not make the host-only test preset fail.
