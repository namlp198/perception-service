# CMake support

Project-specific CMake modules belong here. Dependency downloads are deliberately not performed during
configure; Jetson packages are provisioned explicitly using `scripts/setup.sh`.
